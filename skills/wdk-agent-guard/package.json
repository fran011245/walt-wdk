{
  "name": "@walt-wdk/wdk-agent-guard",
  "version": "1.0.0",
  "description": "Limits and approval flow for autonomous WDK operations — OpenClaw skill",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@walt-wdk/core": "file:../../src/core",
    "ajv": "^8.17.1",
    "proper-lockfile": "^4.1.2",
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "@vitest/coverage-v8": "^4.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}
